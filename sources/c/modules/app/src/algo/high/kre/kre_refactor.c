#include "kre.c"

// link methods
struct KREItem* kre_graph_refactor(struct KREItem* graph);

// implement methods
struct KREItem* kre_graph_refactor(struct KREItem* graph) {
    // TODO: refactor graph (merge same matchers) + convert \s, \S, \w, \W, \d, \D to [...], char, .

    return graph;
}